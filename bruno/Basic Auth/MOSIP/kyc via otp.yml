info:
  name: kyc via otp
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/api/kyc/otp/verify/
  headers:
    - name: Content-Type
      value: application/json
    - name: X-CSRFToken
      value: "{{CSRFToken}}"
  body:
    type: json
    data: |-
      {
        "uid" : "2047631038",
        "txn_id" : "0715399911",
        "otp" : "111111"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
