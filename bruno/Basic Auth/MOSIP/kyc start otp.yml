info:
  name: kyc start otp
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/kyc/otp/start/
  headers:
    - name: Content-Type
      value: application/json
    - name: X-CSRFToken
      value: "{{CSRFToken}}"
  body:
    type: json
    data: |-
      {
        "uid" : "2047631038",
        "use_email" : "true"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
