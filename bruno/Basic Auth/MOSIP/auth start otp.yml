info:
  name: auth start otp
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/api/auth/otp/start/
  headers:
    - name: Content-Type
      value: application/json
    - name: X-CSRFToken
      value: "{{CSRFToken}}"
  body:
    type: json
    data: |-
      {
        "uid" : "2047631038",
        "use_email" : "true"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
