info:
  name: Untitled 1
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8000/api/services/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
